language: python
python:
 - "3.5"
install: 
  - pip install -r requirements.txt
# command to run tests
script: 
  - python test_monitor_sites.py
notifications:
  email:
    on_success: always
    on_failure: always
